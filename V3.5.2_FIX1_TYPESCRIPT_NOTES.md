# V3.5.2 FIX1 — TypeScript preflight fix

修正 `src/services/floatingOverlay.ts` 的 TS7053。

原因：排序 callback 將欄位參數標成 `any`，再用它索引 `Partial<Record<MonitorField, MonitorFieldStyle>>`，TypeScript strict mode 不允許。

修正：
- 匯入 `MonitorField` 型別。
- 先建立 `MonitorField[]` 的 `fields` 陣列。
- 使用型別安全的 `a` / `b` 索引 `fieldStyles` 後排序。

此修正只改 TypeScript 型別與欄位排序寫法，不改功能邏輯、runtimeVersion 或原生 Android 模組。
