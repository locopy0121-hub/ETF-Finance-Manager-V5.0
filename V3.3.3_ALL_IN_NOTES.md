# ETF財務管家 V3.3.3 ALL-IN OTA

Base APK: V3.3.0  
EAS branch: preview  
Runtime version: 3.2.0  

## 本次一次整合

- 六按鈕底部導覽：總覽 / 記帳 / 庫存 / 息收 / 試算 / 設定。
- 設定為正式專屬頁面；NAV 開啟時不再顯示多餘「返回總覽」，NAV 關閉時保留逃生入口。
- 設定頂部分類使用橫向捲動與完整高度，避免文字 / 最後項目裁切。
- 問題 9 即時行情：沿用 V3.3.1/3.3.2 的 TWSE Quote Snapshot 合併與前景刷新穩定化。
- 問題 10 現金資金：升級為獨立核帳系統，新增「系統依流水推算 / 實際核對金額 / 帳務差異」與核帳快照；不會自動竄改 ETF 成本或損益。
- 全局顯示內鎖：關閉後，對應欄位不只從實際卡片隱藏，也從全局卡片設計器欄位池排除。
- 負數貨幣顯示改為 -$133 形式，避免 $-133。

## 全局卡片藍圖正式落地

新增「全局卡片版面設計器」，取代把欄位寬度當成整個版面編輯器的做法。

層級：

全局設定 → 全局卡片 → 頁面 → 5×X / 6×X Grid 整頁畫布 → 卡片 → 單卡資料 / 外觀

功能：

- 頁面切換：資產總覽 / 智慧記帳 / 庫存 / 息收 / 試算 / ETF 詳情。
- 5×X / 6×X Grid；切換欄數時按比例轉換卡片 x / w。
- 卡片直接在畫布拖曳，放開後吸附格線。
- 整列 / 1/2 / 1/3 / 1/4 快速尺寸。
- 精準左 / 右 / 上 / 下、加寬 / 縮窄、增高 / 降高。
- 卡片字體比例、文字靠左 / 置中 / 靠右、背景透明度。
- 每頁專屬欄位池；卡片可包含多個資料欄位。
- +新增卡片、複製卡片。
- SYSTEM_CARD 不可刪除，只能隱藏；CUSTOM_CARD 可刪除。
- 刪除自訂卡只刪版面設定，不刪任何財務資料。
- 復原 / 重做、重設頁面。
- 實際首頁與各頁摘要改讀 pageLayouts，同一份設定直接反映到頁面，不再出現「編輯器有 1/2，但首頁仍全直排」。
- 舊 homeCardFields / pageCardFields 保留做資料遷移與相容，不丟舊資料。

## 驗收重點

1. 首頁卡片在設計器調成整列 + 1/2 + 1/2 後，首頁實際布局應同步。
2. 5×X / 6×X 切換後卡片位置應保持比例，不跳到頁外。
3. 拖曳卡片放開後應吸附格線。
4. 系統卡無刪除按鈕；自訂卡可刪，財務資料不受影響。
5. 關閉 NAV / 淨值、今日損益、總損益、股息、即時行情或更新時間後，對應欄位從卡片顯示與新增欄位池一起消失。
6. 現金資金核帳：實際金額 − 系統推算 = 帳務差異；只記錄核帳，不自動調帳。
