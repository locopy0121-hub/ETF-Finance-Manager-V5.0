# ETF財務管家 V3.3.10 — Chart + Widget Hotfix

## 本版目標
以已成功發布的 V3.3.9 / runtime 3.2.0 為基底，追回 V3.3.7 藍圖「資料 + 圖表並行」的首頁設計，同時把圖表引擎擴充為可嵌入單卡，並實作 Android Widget 走勢圖。

## 已完成
- 首頁第一張核心卡預設為全寬「數據 + 面積走勢圖」，資料與圖表並行。
- 圖表卡支援折線圖 / 面積圖。
- 系統卡、自訂卡可直接開啟「內嵌走勢圖」，不必另建圖表卡。
- 混合卡支援：左數據右圖表、左圖表右數據、上數據下圖表、上圖表下數據。
- 小尺寸卡片自動縮放字體與欄位寬度，避免文字與金額互相覆蓋。
- 息收日曆固定在息收頁 ScreenTitle 後第一區，不加入可拖曳卡片 Deck，因此不會被移到下方。
- Android Widget 新增 SvgWidget 迷你走勢圖，可選總損益 / 今日損益 / 總資產 / 持股市值，以及盤中 / 每日歷史資料。
- Widget App Snapshot、手動 TWSE 更新、背景更新都保留 / 延續走勢資料。
- UI 版本字串改用 `src/v3/version.ts` 單一來源，避免首頁與設定頁漏改舊版本。
- V3 state schema 升為 5，舊 raw key `totalInvestedCost` 自動遷移成 `historicalCashOutflow`。
- `totalAssets` 維持 V3.3.8 起的正式帳務契約：投資總資產 = 持股市值；現金為獨立核帳欄位，不偷偷加回投資總資產。

## OTA 相容性
- App version: 3.3.10
- Runtime version: 3.2.0（保持不變）
- Branch: preview
- Environment: preview
- Platform: android

## 發布
直接執行：

```powershell
.\PUBLISH_V3.3.10_OTA.cmd
```

腳本會先跑 dependency check、TypeScript、Accounting Contract、Engine Runtime；任一失敗都不發布 OTA。
