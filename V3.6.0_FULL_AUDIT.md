# V3.6.0 全 APP 總體稽核

## 已發現並納入修正的系統性問題

| 區域 | 稽核疑點 | V3.6.0 處理 |
|---|---|---|
| Global Editor | 編輯器可儲存但真正卡片未即時套用 | 個別欄位 Save 直接 commit pageLayouts；Renderer 補 effects |
| 浮動監視器 | JS fieldStyles 已存在、Native Overlay 未解析 | Kotlin `styledFieldText()` 統一解析 fieldStyles |
| 桌面鎖 | 主體鎖定但縮放把手仍可改大小 | drag / resize 都檢查 locked；鎖定時不顯示 resize grip |
| 拼圖 | 高度不足時內容容易被裁切 | puzzle 使用內容高度、自動增加卡片內容；保留最大顯示數 |
| 排序 | custom 順序只存在設定層 | fields 依 order 排序；ETF rows 依 selectedSymbols 自訂順序 |
| 排程 | JS 只有 App lifecycle，背景跨時段不完整 | Native AlarmManager Receiver + BOOT_COMPLETED 恢復 |
| 設定頁 | 第二層分類文字被框裁切 | minHeight / padding / lineHeight / horizontal scroll 強化 |
| 首頁圖表 | 單擊切換狀態沒有可靠視覺回饋 | 第一次 tap 立即 cycle，提示目前類型，AsyncStorage 記住樣式 |
| AI 配息 | 「最近一次」沒有嚴格 scope | intent parser + latest/lastN/year/history/next |
| AI BOT | 舊跨 App BOT 欄位仍殘留在模型 | 舊 React BOT 元件移除；AI preferences 回歸 AI 本身 |
| Android 編輯 | Navigation Bar 會遮底部按鈕 | Native immersive navigation + Safe Area 雙層處理 |
| App Icon | 無法在 App 內切換 | 10 aliases + Native switcher + Icon Center |
| Launcher | 常用功能入口太深 | 長按 App Icon：新增買進、監視器、今日損益、股息、AI |
| Safe Area | 舊 RN SafeAreaView 風險 | audit 強制禁止從 react-native 匯入 SafeAreaView |

## 財務/資料層檢查
- 交易成交金額仍走既有 `preciseTradeAmount` 規則，沒有因 UI 改版改動財務公式。
- Global Editor 只改顯示設定，不寫入持倉、帳務或股息正式資料。
- AI 寫入真實資料仍保留確認流程；一般查詢不自動建立股息事件。
- Widget / Monitor 使用同一套 holdingMetrics / portfolioMetrics 計算結果，減少市值/損益來源分裂。
- Schema 升為 12；AI 舊 floating 欄位以白名單遷移方式清除，不把舊 Cross-App BOT 設定帶入新版本。

## 自動 audit
`scripts/V360_NATIVE_COMPLETE_AUDIT.cjs` 共 36 項，檢查版本、Schema、Global Editor 綁定、設定頁、自動隱藏導航列、Chart、AI、Legacy BOT 清理、Native Monitor、排程、10 Icons、快捷入口與 SafeAreaView。

## 仍需「實機」驗收的項目
程式碼 audit 不能取代 Android 實機：activity-alias 切換後 Launcher 快取、不同廠牌的導航列/手勢列、Overlay 背景服務限制、開機 Receiver、Widget 雙擊時間窗，以及各家 Launcher 對 shortcut/icon 更新速度必須在 V3.6.0 APK 實機確認。
