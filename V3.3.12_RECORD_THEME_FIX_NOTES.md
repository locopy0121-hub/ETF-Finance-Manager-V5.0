# ETF財務管家 V3.3.12 — 記帳 / 佈景卡片修正

## 1. 智慧記帳：交易紀錄與市場行情完全解耦
- 「新增買進 / 賣出」是歷史交易紀錄，不是即時下單。
- 交易日期改用獨立的 `dateText` 狀態保存；使用者選定過去日期後，ETF 查詢或行情刷新不得改回今天。
- 「成交行情」文字統一改為「成交價格」。
- 成交價格預設留白，必須依券商實際成交紀錄輸入。
- 輸入 ETF 代號仍會自動辨識 ETF 名稱。
- TWSE 最新價格只顯示為「市場參考行情（不寫入交易）」，不會覆寫成交價格、交易日期、股數或費用。
- 編輯歷史帳務與庫存購入紀錄也統一使用「成交價格」語意。

## 2. 設定 → 全局卡片：修復空白頁
V3.3.11 的設定 Tab 已有 `cards` 項目，但缺少 `section === 'cards'` 的實際渲染內容，因此進入後整頁空白。

V3.3.12 新增：
- 顯示目前已套用的佈景名稱。
- 顯示目前佈景的卡片透明度、圓角、字體比例與強調色。
- 可切換查看「資產總覽 / 智慧記帳 / 庫存清單 / 股息日曆 / 時空試算 / ETF 詳情」各頁卡片。
- 列出每張卡片的類型、寬度、欄位、顯示/隱藏狀態與圖表設定。
- 可直接開啟全局拖曳卡片設計器。
- 可進入各頁欄位 / 比例編輯器。

## 3. 主題與卡片的正確關係
- 按「確認並套用此全局佈景」時，同步把該主題的卡片透明度、圓角、字體比例套到現有卡片。
- **不清空、不重建** 使用者的卡片內容、欄位、順序、寬度、位置與圖表資料來源。
- 「載入目前佈景的卡片外觀設定」可隨時重新把主題外觀套回卡片，但仍只改外觀。

## 4. 版本契約
- App version: 3.3.12
- runtimeVersion: 3.2.0
- Android package: com.etfpilot.twselive
- Android versionCode: 32
- 無新增 native dependency，維持既有 APK runtime 契約。
