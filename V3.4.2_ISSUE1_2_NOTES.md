# ETF 財務管家 V3.4.2 — 問題 1＋2 完整整合說明

## 問題 1：Finance Engine 2.0 / 全局資料 / 自訂公式

### 核心成本規則
- 每筆買進純成本：`floor(成交行情 × 股數)`。
- 每筆先捨去小數，再把各筆相加。
- 總成本不包含買進手續費。
- 手續費繼續保存並可由「含費成本 / 含費損益」獨立呈現。

### 市值與損益
- 單檔即時市值 = `即時行情 × 目前持股數`。
- 單檔庫存即時損益 = `單檔即時市值 - 單檔純成本`。
- 總市值 = 所有持有 ETF 即時市值加總。
- 損益試算 = `總市值 - 總成本`。
- 今日損益保持獨立：`(即時行情 - 昨收) × 目前持股數`。

### Chronological cost basis
買賣歷史依時間處理，賣出使用當時的移動平均成本；後來新增的買進不回頭污染過去已賣出的成本基礎。

### Global Metrics 2.0
新增統一資料層，提供值、來源、公式、資料時間與 stale 狀態，供 APP、自訂資料框與 BOT 使用。

### 可編輯標題 / 自訂資料框
- 主要頁面標題可自訂。
- 可新增自訂公式欄位。
- 自訂欄位可加入頁面卡片資料池。
- 公式只讀取資料並呈現，不覆寫原始帳務。

### Safe Formula Engine
不用 `eval` / `new Function`，支援：
- `+ - * / %`
- `> < >= <= == !=`
- `()`
- `SUM AVG MIN MAX IF ROUND FLOOR CEIL ABS`
- 可引用全局欄位與 `[ETF.欄位]`。

## 問題 2：跨 APP AI BOT 2.0

### 原生表格 UI
Overlay 不再只是單一文字區塊，改為 Android 原生表格式 UI：
- Header：AI BOT / 市場狀態 / 時間 / 設定 / 固定 / 關閉。
- Table：使用者選擇 ETF 與欄位。
- Summary：總成本 / 總市值 / 損益試算 / 成本報酬率。
- Footer：資料來源 / 更新時間 / 連線狀態。

### 可選即時欄位
ETF代號、名稱、即時行情、漲跌、漲跌幅、持有股數、即時市值、純成本、庫存即時損益、庫存報酬率、今日損益、今日損益率、昨收、開高低、成交量、更新時間等。

### 自訂版面
- 自選欄位。
- 固定欄位。
- 顯示行數 1–30。
- ETF 代號樣式可跟隨庫存損益色 / 固定底色 / 外框 / 純文字。
- BOT 背景、文字、框線、圓角可調。

### 損益顏色
- 正數：台股紅。
- 負數：綠。
- 0 / 無資料：中性。
- 判色直接取該欄位自己的數值，不用行情漲跌猜。

### APP / 跨 APP BOT 互斥
- APP foreground：立即 stop native overlay，只顯示 APP 內 BOT。
- APP background/inactive：延遲確認後才啟動/更新同一 native overlay service。
- 原生 render 每次重建同一個 root，不累積重複 view。

### 0 秒安全更新
0 秒不是 `setInterval(0)`；原生層轉為約 1000ms 安全即時模式，非零更新最低間隔 250ms。

## Native baseline
因本版變更 Kotlin Overlay Service，首次使用必須安裝 V3.4.2 APK。V3.4.1 runtime 不可直接靠 OTA 取得問題 2 的原生功能。
