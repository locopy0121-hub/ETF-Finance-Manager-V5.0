# ETF 財務管家 V3.4.1 — 問題 1～10 完整整合版

## 發布基線
- App version: `3.4.1`
- Runtime version: `3.4.1`
- Android versionCode: `34`
- Android package: `com.etfpilot.twselive`
- Expo SDK: `57`
- 此版含跨 APP 系統懸浮 BOT 原生模組行為更新，因此 **第一次必須重新建置並安裝 V3.4.1 APK**。裝置進入 runtime 3.4.1 後，後續相容的 JS/UI 修改才可用 OTA。

## 問題 1 — APP 內 AI BOT
- APP 內 BOT 預設以 🤖 圖示呈現；單擊展開，雙擊/連點收合。
- 寬度、高度分離自由調整，保留使用者尺寸與位置。
- 支援內容自動高度、字體自適應；內容超過可視高度時才使用內部捲動。

## 問題 2 — 跨 APP 系統懸浮 BOT
- APP 在前景時跨 APP BOT 自動隱藏；APP 退到背景/切換其他 APP 時才顯示。
- 更新秒數支援 `0` 到任意值；`0` 為安全即時模式，不使用無限 `setInterval(0)`。
- 支援自訂寬高、字體自適應、最小/最大字級等設定。

## 問題 3 — APP 圖表互動
- 小圖表單擊：切換下一種可用圖表樣式。
- 小圖表雙擊：開啟放大視窗；雙擊優先，避免第一下誤切換。
- 放大視窗支援資料點選取、期間切換、縮放與水平檢視；按「離開」回原頁。
- 小圖與放大圖共用同一份資料。

## 問題 4 — Android Widget 圖表
- Widget 使用專用 compact 趨勢圖，不直接硬縮 APP 大圖。
- 支援 line / area / bar / sparkline / step 等顯示。
- 可設定網格、座標、最後值、百分比、更新時間、圖高、線寬、字體與透明度。

## 問題 5 — 全局數值顯示
- 金額/百分比/一般數值支援智慧顯示、固定 0～4 位與「自由設定」。
- 智慧顯示會移除無意義 `.00` / 尾端 0，但只影響顯示，不改底層計算精度。
- 行情/成交價格/最新價硬性固定為 `XXXX.XX` 兩位小數。
- 全局金額資料移除 `$` / `NT$` 前綴。

## 問題 6 — 月曆與生活感加薪
- 月曆加入設定模式，可跟隨全局主題或獨立覆寫。
- 可調背景、日期字、事件色、週末色、格線、圓角、格高、密度、今日/選取樣式等。
- 股息頁「生活感加薪進度」加入顯示、標題、目標、外觀、動畫與主題設定，進度使用實際股息資料。

## 問題 7 — 紀錄/庫存/現金/損益安全管理
- 清除、重建、重新計算等高風險操作會先建立安全備份；備份失敗即阻止操作。
- 支援歷史備份、還原；執行還原前也會再建立救援備份。
- 重新計算會保護沒有完整舊買進 ledger 的既有持倉，避免誤清空。

## 問題 8 — 持有試算與付出 VS 未來資產
- 可從目前持有 ETF 任意勾選多檔建立試算自訂組合。
- 比例與配置金額雙向換算，支援依現有持倉比例與平均分配。
- 試算不修改真實庫存。
- 付出 VS 未來資產支援多種圖表樣式、單擊換樣式、雙擊放大、資料點細節、縮放與期間檢視。

## 問題 9 — 系統跑馬燈
- 跑馬燈完整納入設定，可選內容來源、方向、速度、停留、字體、透明度、圓角與動畫強度。
- 動畫包含 scroll / pingpong / center / blink / jump / pulse / wave / bounce / scale / fade。
- 提供減少動態設定。

## 問題 10 — 當日損益明細
- ETF 代號框顏色只依該檔「實際當日損益」正負判定：正值台股紅、負值台股綠、0/缺資料中性。
- 代號框與損益數字使用同一語意色；不可用行情漲跌猜測損益色。
- 收盤/行情固定兩位小數，例如：`[綠底 0050 白字] XXXX股 · 收盤 109.15  -16.00`。
- 正/負/中性色可在設定內自訂，但正負判定原則不可改。

## 建置
在 Windows PowerShell、專案目錄執行：

```powershell
powershell -ExecutionPolicy Bypass -File .\BUILD_V3.4.1_APK.ps1
```

或：

```powershell
powershell -ExecutionPolicy Bypass -File .\BUILD_APK.ps1
```

## OTA
必須先有 V3.4.1 APK/runtime 基線：

```powershell
powershell -ExecutionPolicy Bypass -File .\OTA_V3.4.1_PREVIEW.ps1
```
