# ETF 財務管家 V3.3.14｜購入金額小數修正

## 本次修正
使用者實機發現購入紀錄金額 `1,583.68` 會顯示 / 保存成整數（例如 `1,584`）。本版直接修正成交金額與顯示格式的兩個根因。

### 1. 成交金額不再整數化
- `成交金額 = 成交價格 × 股數`
- 最終保留 2 位小數。
- 不再使用 `Math.trunc()` 捨去小數元。
- 範例：`15.68 × 101 = 1,583.68`，必須保持 `1,583.68`。

### 2. 金額顯示固定 2 位小數
- 購入成本、成交金額、手續費、含費總成本等金額顯示固定 2 位小數。
- 不再使用 `Math.round()` 把 V3 金額顯示成整數。
- `1,583.68` 不得顯示成 `1,584`。

### 3. 新增 / 修改購入紀錄同步修正
- 新增 ETF 購入紀錄：成交金額、手續費、含費總成本保留 2 位。
- 智慧記帳買進紀錄：成交金額保留 2 位。
- 庫存 → 修改購入紀錄：本筆成交金額與本筆現金支出保留 2 位。
- 舊版庫存修改畫面也移除整數化。

## 保留既有 V3.3.13 規則
- 交易日期由使用者手動選擇。
- 成交價格由使用者手動輸入。
- ETF 代號只負責帶入 ETF 名稱。
- 新增買進紀錄不讀取即時行情來修改日期或成交價格。

## Runtime
- App version: `3.3.14`
- runtimeVersion: `3.2.0`
- Android package: `com.etfpilot.twselive`
