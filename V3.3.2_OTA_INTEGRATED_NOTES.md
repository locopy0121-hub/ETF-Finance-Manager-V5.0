# ETF財務管家 V3.3.2 OTA 整合版

此版本以 V3.3.0 APK 為原生基底，整合先前 V3.3.1 OTA 預製內容，並補上實機測試發現的「設定缺少專屬頁面」問題。

## 問題 9｜即時行情資料引擎
- TWSE MIS 核心行情來源保留。
- 行情刷新採成功快照合併，單一 ETF 暫時缺值時不清空其他已成功行情。
- 保留前景立即刷新、盤中更新頻率、盤外快照與 Widget 共用行情架構。

## 問題 10｜現金資金獨立核帳
- 「提存現金 / 現金提存」統一命名為「現金資金」。
- 流水支援券商、帳戶 / 交割戶與核帳備註。
- 定位為獨立核帳，不自行改 ETF 成本與投資損益。
- 保留系統推算 vs 實際餘額 vs 差異的後續擴充基礎。

## 實機修正｜設定專屬頁面 + 六按鈕 NAV
- 底部導覽正式改為 6 個入口：總覽、記帳、庫存、息收、試算、設定。
- 「設定」不再只靠彈出 Modal；新增專屬全頁設定中心。
- 原各頁右上設定按鈕會直接切換到設定專頁。
- 設定專頁保留主題、全局顯示內鎖、卡片資料、市場數據、券商、Widget、盤後訊息、AI、OTA、資料備份等原功能。
- 設定頁內提供「返回總覽」。即使使用者關閉底部 NAV，也不會被困在設定頁。
- 六按鈕底部導覽縮小圖示 / 字級並保持等寬，避免小螢幕擠壓。

## OTA 相容性
此整合未新增原生套件、未修改 app plugin / native module，目標直接從已安裝的 V3.3.0 APK 透過 preview branch OTA 更新。

## 發布
PowerShell：

```powershell
cd "你的專案資料夾"
.\OTA_V3.3.2_PREVIEW.ps1
```
