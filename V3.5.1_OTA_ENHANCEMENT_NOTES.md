# ETF財務管家 V3.5.1 OTA－完整加強優化版

## 發布基線
- App version：`3.5.1`
- OTA runtime：`3.5.0`
- Android package：`com.etfpilot.twselive`
- Android versionCode：`36`（OTA 不變）
- 原生基線：**V3.5.0 FIX1 APK**
- 本版不修改 AndroidManifest、Kotlin Overlay、Widget Provider 或 native dependency。

## 問題 1～5 整合
1. **即時監控器**：沿用 V3.5.0 FIX1 的原生「單擊不回 App」行為；OTA 同步把舊 `openApp` 設定自動遷移成 `none`，A/B 雙擊切換保留。
2. **各頁自訂模式**：移除內容區頂部大型開關，改成頁面標題列的鎖定/解鎖入口；每頁獨立保存。
3. **設定中心**：主分類固定在上方橫向左右切換，採「圖示＋文字」；下方只顯示該分類的文字小項目；下拉/選值元件只負責設定值。
4. **表單鍵盤避讓**：記帳、庫存修改、股息、試算、設定等輸入區加入 keyboard-aware 行為，降低底部欄位被鍵盤遮住。
5. **庫存頁強化**：新增整體庫存總覽、持股市值、現金帳、今日/總損益、ROI、累積配息、含息綜合損益、待入帳股息、損益貢獻排行與最近交易/股息摘要。

## OTA 原功能強化
- 首頁新增 **市場總覽 / 熱門 ETF / 漲跌排行 / 成交量**。
- 新增 **ETF 代號 / 名稱搜尋**與安全類型篩選，並直接加入既有自選清單。
- ETF 詳情以 TWSE 歷史 OHLC 計算：1週、1月、3月、1年報酬、年化波動率、最大回撤、簡化 Sharpe（RF=0）。
- 歷史行情抓取上限擴充至約 18 個月，支援較完整年度統計。
- 保留既有 Finance Engine、股息日曆、時空試算、Widget、即時監控器、卡片系統與使用者資料。

## 金融資料原則
本版不以假資料補欄位。若沒有可靠來源，會明確顯示「資料不足 / 待串接」：
- Beta（需要可靠基準/追蹤指數序列）
- 費用率、完整成分股、產業/地區配置
- 無可靠來源時的 NAV 歷史、殖利率與基金規模

## 發布方式
手機必須先安裝 V3.5.0 FIX1 APK，且 runtime 為 `3.5.0`。

在 Windows PowerShell 於專案根目錄執行：

```powershell
powershell -ExecutionPolicy Bypass -File .\OTA_V3.5.1_PREVIEW.ps1
```

腳本會先執行 V3.5.1 preflight，再發佈 Android preview OTA。
