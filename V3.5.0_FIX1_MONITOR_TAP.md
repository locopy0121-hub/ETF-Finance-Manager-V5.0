# ETF財務管家 V3.5.0 FIX1 — 即時監控器點擊修正

## 修正
- 即時監控器單擊不再啟動主 App。
- 保留雙擊 A/B 最愛尺寸切換。
- 鎖定模式下點擊也不會啟動主 App。
- 舊設定若仍保存 `tapAction=openApp`，載入時會自動遷移為 `none`。
- 傳給原生 Overlay 的 payload 強制使用 `tapAction=none`，避免舊快取/舊 Profile 再觸發開 App。

## 行為
- 單擊監控器：不跳 App。
- 雙擊監控器：A/B 尺寸切換（啟用時）。
- 拖曳：移動監控器。
- 右下角拖曳：縮放。
- 回 App：後續只允許專屬按鈕/明確動作，不綁定整個監控器表面。
