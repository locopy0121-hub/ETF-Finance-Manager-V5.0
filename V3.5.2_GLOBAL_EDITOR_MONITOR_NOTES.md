# ETF財務管家 V3.5.2 OTA

## 一次整合範圍
- Global Editor 2.0：監視器欄位採獨立彈出式編輯器，支援順序、字體大小/粗細、字色、底色、透明度、損益色、對齊、內距、圓角、陰影/發光/描邊與儲存前即時預覽。
- Floating Monitor 2.0：自訂顯示順序、桌面版面鎖、拼圖高度保護、排程設定、Widget/AI BOT 設定解耦。
- Settings Layout 2.0：設定頁小項目改為自適應高度、增加垂直 padding / lineHeight，降低文字被裁切。
- AI Cleanup：AI 助理設定頁移除跨 APP BOT 舊入口；跨 App 顯示統一由「監控工具 → 浮動即時監視器」負責。

## OTA 邊界
此包的 runtimeVersion 維持 3.5.0，發布腳本只執行 EAS Update，不重建 APK。
現有 APK 的 Native Overlay 已支援 locked / puzzle / resize 等既有 payload；新增的逐欄 fieldStyles 已寫入 JS payload 與設定資料。若目前手機內的 Native Overlay 版本尚未解析 fieldStyles，App 內編輯與預覽會保存設定，但桌面 Overlay 的逐欄樣式需等下一個 Native APK 才能完整渲染。這是 Expo OTA 的原生碼限制，發布腳本不會偷偷更換 APK。

## 發布
PowerShell：`powershell -ExecutionPolicy Bypass -File .\\OTA_V3.5.2_PREVIEW.ps1`
