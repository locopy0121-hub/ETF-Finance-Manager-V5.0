# ETF財務管家安裝指南

1. 安裝 Node.js 22 LTS。
2. 在專案資料夾執行 `npm install`。
3. Expo/EAS 登入後執行 `powershell -ExecutionPolicy Bypass -File .\BUILD_APK.ps1`。
4. EAS preview profile 會輸出 Android APK 安裝連結。
5. 此專案 runtimeVersion 為 1.2.0；V1.2.0 APK 可接收相同 runtime 的 OTA。
