# ETF財務管家 V1.5.4 — Google Vision OCR Only

- 保留「智慧掃描新增庫存」的拍照、相簿選圖、圖片掃描、候選預覽、數學核實與批次加入流程。
- 移除 App 執行流程中的本機 ML Kit OCR；掃描後只呼叫 Google Cloud Vision Worker。
- `rn-mlkit-ocr` 原生套件仍留在既有 V1.5.0 基準 APK 中，但 V1.5.4 JavaScript 不再呼叫它，因此本次可直接 OTA，不需重建 APK。
- Google API Key 只保存在 Cloudflare Worker Secret；App 僅保存 Worker URL 與 OCR_SHARED_TOKEN。
- runtimeVersion 維持 1.5.0。
