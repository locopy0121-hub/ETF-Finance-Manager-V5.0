# ETF財務管家 V3.5.0 — 即時監控器 + Widget 原生升級

## 目標
以 V3.4.2 Issue 1+2 FINAL 為唯一母版，不另起新專案。V3.5.0 將 Cross-App BOT 正式改名為「即時監控器」，並建立 App 看板 / 即時監控器 / Android Widget 共用設定語言、各自獨立 Profile 的架構。

## 核心完成項目
- 跨 App 原生 Overlay 改為即時監控器。
- Overlay 單一 Service / 單一 root 重用。
- 右下角拖曳縮放；寬高限制；8dp 預設格線吸附；保存寬高與位置。
- 雙擊切換精簡 / 展開尺寸。
- 靠邊與半隱藏 peek。
- 鎖定模式避免誤觸。
- 統一 MonitorProfile：App 看板 / floating / widget 三份獨立設定。
- 智慧 / 列表 / 拼圖三種呈現模式資料模型。
- Fluid 內容自適應與 Scale 等比例縮放模式。
- 多檔 ETF、欄位池、排序、透明度、圓角、分隔線、斑馬紋、更新頻率、警示門檻、冷卻時間。
- 設定中心改成大字手風琴群組。
- 即時監控器設定採「啟用」與「開放設定模式」雙開關。

## Widget 定位
Android Widget 受 Launcher cell grid 管理；V3.5.0 不假裝讓 App 自己控制桌面 resize handle。Widget 用來顯示快照與啟動入口，真正跨 App 即時損益使用 Overlay。三者共用欄位/視覺設定語言，但各自保存配置。

## 建置
這版含 Kotlin 原生變更，不能只發 OTA。必須先建置並安裝 V3.5.0 APK。

```powershell
powershell -ExecutionPolicy Bypass -File .\BUILD_V3.5.0_APK.ps1
```

完成 V3.5.0 APK 安裝後，後續只改 JS/UI 且不碰 native/runtime 才可：

```powershell
powershell -ExecutionPolicy Bypass -File .\OTA_V3.5.0_PREVIEW.ps1
```
